(define (rational a b)
  (a . b))
(define (numerator r)
  (define (sign n)
    (cond ((= n 0) 0)
	  ((< n 0) -1)
	  ((> n 0) 1)
	  ))
  (* (abs (/ (car r) (gcd (car r) (cdr r)))) (sign (* (car r) (cdr r))))
(define (denominator r)
  (if (= (car r) 0)
      1
      (gcd (car r) (cdr r))))
